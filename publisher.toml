repository = "termapps/crude"

exclude = ["aur"]

[homebrew]
repository = "termapps/homebrew-tap"

[aur_bin]
name = "crude"

[scoop]
repository = "termapps/scoop-bucket"

[nix]
repository = "termapps/nixpkgs"

lockfile = false
path     = "%n.nix"
